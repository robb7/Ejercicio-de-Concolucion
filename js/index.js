function calConvolucionPeriodica(){

  var x = document.convPeriodica.operandoX.value;
  var y = document.convPeriodica.operandoY.value;
  var result = parseInt(x) + parseInt(y);
  document.convPeriodica.resultado.value = result;
}
